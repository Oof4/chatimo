require('security.lock')
Security 
Complete security.lock

{
if done

end;
}
end;

else;
use ('reset.json')
if success = run();
if failed success
use ('reset.json')
end;

else;
if success = false
if error = 258 - Couldn't Block At Security.lock And Function(); block.json fail  = end;
use debugerror.json
if debugerror.json  use = success 
end;
run();
if debugerror.json  , reset.json stop();
stop();
reset();
run();



--------------------------------------------
File.IGNORE
--------------------------------------------

Please Do Not Touch This And Edit It's Stuff. This Is Needed To Block Tracks And Function security.json,
Thanks.

Also, If You Haven't Already, Update CHATIMO Now.
